# Dependencies for the Vercel deployment (Vercel installs from requirements.txt,
# not uv.lock). Direct dependencies only — pip resolves the rest.
#
# Deliberately omits anthropic, langsmith, and voyageai: the deployed demo runs
# LLM_PROVIDER=gemini / EMBEDDING_PROVIDER=gemini, and those three are imported
# lazily inside their provider functions so they never load here. Skipping them
# drops ~45 MB of transitive weight (pillow, tokenizers, hf-xet, aiohttp) from a
# bundle that has a 250 MB ceiling.
#
# Keep version floors in sync with pyproject.toml when they change there.

fastapi>=0.110
uvicorn[standard]>=0.30
jinja2>=3.1
pydantic>=2.5
pydantic-settings>=2.0
sqlalchemy>=2.0
psycopg[binary]>=3.2
pgvector>=0.3
google-genai>=1.0
httpx>=0.27
beautifulsoup4>=4.12
pyyaml>=6.0
apscheduler>=3.10
